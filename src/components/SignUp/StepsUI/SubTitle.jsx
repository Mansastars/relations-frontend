import React from "react";

const SubTitle = ({ subTitle }) => {
  return (
    <div className=" text-center text-base text-wrap text-gray-500">
      {subTitle}
    </div>
  );
};

export default SubTitle;
